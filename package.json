{
  "name": "sdd-monorepo",
  "private": true,
  "author": "Bruno Fortunato <bruno.fortunato@applica.guru>",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @applica-software-guru/sdd-core && npm run build -w @applica-software-guru/sdd && npm run build -w sdd-vscode",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.3.3",
    "typescript": "^5.9.3",
    "vitest": "^3.0.0"
  },
  "version": "1.8.4"
}
